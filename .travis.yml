sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docer-compose exec --env 'RAILS_ENV=test' web rails db:create
  - docer-compose exec --env 'RAILS_ENV=test' web rails db:migrate
  - docer-compose exec --env 'RAILS_ENV=test' web rails test